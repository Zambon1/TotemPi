# TotemPi

This is an on-going project being created to allow a user to deploy a calendar+tasks management system through a command line interface. The user will be able to update and add tasks via a web interface built with React, with the changes being reflected on the command line interface deployed on a small computer such as a Raspberry Pi.
